/**
 * @file tdx_version.h
 * @brief TDX / librex version (CLI -v / --version).
 */
#ifndef TDX_VERSION_H
#define TDX_VERSION_H

/** Product version; bump on meaningful behavior change. */
#define TDX_VERSION_STRING "0.11"

/** Reusable debug-core ABI version (same series as TDX). */
#define REX_VERSION_STRING "0.11"

#endif /* TDX_VERSION_H */
